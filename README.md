# خودت «فناوری موردنظرت» را بساز

![Banner](https://codecrafters.io/images/new-ccgithub-banner.png)

این مخزن مجموعه‌ای از راهنماهای خوش‌نویس و گام‌به‌گام برای بازسازیِ از صفرِ فناوری‌های محبوب است.

> *هر چیزی را که نتوانم بسازم، نمی‌فهمم — ریچارد فاینمن.*

## 📖 ترجمه کامل فارسی

نسخه کامل و ترجمه‌شده این مخزن (همه آموزش‌ها، دسته‌بندی‌ها و لینک‌ها) در فایل زیر قرار دارد:

- 👉 [**README-fa.md — ترجمه کامل فارسی**](README-fa.md)

نکته: برای مشاهده بهتر متن فارسی این مخزن را با پشتیبانی RTL احراز کنید (همه محتوای مارک‌داون فارسی در فایل README-fa.md قرار دارد).

## پلتفرم آموزشی BYOX

پلتفرم HTML فارسی (پوشه `byox/`) که همه آموزش‌ها را به‌صورت صفحه‌به‌صفحه، دقیق و کامل ترجمه می‌کند — هر بار ۱۰ آموزش.

- 🌐 **آدرس آنلاین:** `https://hordekiller.github.io/build-your-own-x/`
- 🚀 با هر push روی شاخه `master`، به‌صورت خودکار روی **GitHub Pages** دِپلوی می‌شود (workflow: `.github/workflows/deploy-pages.yml`).

**نکته فعال‌سازی (یک بار):** از تب *Settings ← Pages* مخزن، بخش «Build and deployment» را روی *GitHub Actions* بگذارید تا دِپلوی خودکار فعال شود.

هر آموزش شامل: ترجمه کامل مقاله، بلاک‌های کد با کامنت فارسی، تصاویر با آدرس سایت اصلی و بلاک کپیرایت + لینک منبع. آموزش‌های ویدیویی (یوتیوب) فقط لینک اصلی ویدیو را نگه می‌دارند.